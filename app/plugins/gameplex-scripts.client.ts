// Clean placeholder plugin replacing previously corrupted script loader.
// If you need to reintroduce external scripts, prefer installing via npm or
// using <script> tags in app.html rather than imperative DOM injection here.
export default defineNuxtPlugin(() => {
  // intentionally left minimal to resolve build error
});
